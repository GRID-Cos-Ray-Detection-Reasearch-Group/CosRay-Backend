log:
  level: INFO

entryPoints:
  web:
    # http
    address: ":80"

  web-secure:
    # https
    address: ":443"

certificatesResolvers:
  letsencrypt:
    # https://doc.traefik.io/traefik/https/acme/#lets-encrypt
    acme:
      email: "grid-cos-ray-detection-reasearch-group@example.com"
      storage: /etc/traefik/acme/acme.json
      # https://doc.traefik.io/traefik/https/acme/#httpchallenge
      httpChallenge:
        entryPoint: web

http:
  routers:
    web-router:
      rule: "PathPrefix(`/`)"
      entryPoints:
        - web
      middlewares:
        - csrf
      service: django

    web-media-router:
      rule: "PathPrefix(`/media/`) || PathPrefix(`/static/`)"
      entryPoints:
        - web
      middlewares:
        - csrf
      service: django-media

    web-secure-router:
      rule: "PathPrefix(`/`)"
      entryPoints:
        - web-secure
      middlewares:
        - csrf
      service: django
      tls: {}

    web-secure-media-router:
      rule: "PathPrefix(`/media/`) || PathPrefix(`/static/`)"
      entryPoints:
        - web-secure
      middlewares:
        - csrf
      service: django-media
      tls: {}

  middlewares:
    csrf:
      # https://doc.traefik.io/traefik/master/middlewares/http/headers/#hostsproxyheaders
      # https://docs.djangoproject.com/en/dev/ref/csrf/#ajax
      headers:
        hostsProxyHeaders: ["X-CSRFToken"]

  services:
    django:
      loadBalancer:
        servers:
          - url: http://django:5000

    django-media:
      loadBalancer:
        servers:
          - url: http://nginx:80

providers:
  # https://doc.traefik.io/traefik/master/providers/file/
  file:
    filename: /etc/traefik/traefik.yml
    watch: true
